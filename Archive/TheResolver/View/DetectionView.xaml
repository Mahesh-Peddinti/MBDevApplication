<UserControl x:Class="TheResolver.View.DetectionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:TheResolver.View"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <StackPanel Margin="10">
            
            <Button Content="Run Clash Detection" 
                    Command="{Binding RunDetectionCommand}" />

            <ListBox ItemsSource="{Binding FilteredResults}" 
                     Height="250">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <CheckBox                            
                            Content="{Binding Description}" />                        
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>              

            <StackPanel Orientation="Horizontal" Margin="0,10,0,0" >
                <Button Content="Mark Resolved" Margin="1" Command="{Binding MarkResolvedCommand}" />
                <Button Content="Mark Ignored" Margin="1" Command="{Binding MarkIgnoredCommand}" />
                <Button Content="Generate Suggestions" Margin="1" Command="{Binding GenerateSuggestionsCommand}"/>
                <Button Content="Export Report" Margin="1" Command="{Binding ExportReportCommand}" />
            </StackPanel>

            <TextBlock Text="Resolution Suggestions:" 
                       FontWeight="Bold" 
                       Margin="5"/>
            <ItemsControl ItemsSource="{Binding SelectedClash.Suggestions}" />
            <Button Content="Run Resolver" VerticalAlignment="Bottom"
                    Command="{Binding ResolveClashCommand}" />

        </StackPanel>
    </Grid>
</UserControl>
